// main.rs

/*
 * Linguaggio:
 * 
 * print?() -> funzione (inizialmente, solo per adesso pk non so come fare) poi macro
 * println?() -> funzione, poi macro anche questa
 * 
 * let -> dichiarazione variabili immutabili
 * let x: mod = 10 per adesso solo unsigned int
 */

mod lexer;
mod parser;
fn main() {
    
}