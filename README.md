# LampScript-Compiler
The official compiler for the LampScript Lang (LAS)

## What is LampScript
LampScript is a simple programming language that supports these simple operations:
```rs
let x = 10; // supports u64

print?(); // now a simple function, in future will be a macro
print?(x);
```
